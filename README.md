# deep_larning-iris

# Creating model with TensorFlow and Keras 

Main purpose of this model is to make predictions based on a given input data, what kind of iris flower is this
 
To run the script, execute the following command in your terminal:

``python iris_Model.py``